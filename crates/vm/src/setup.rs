use crate::VM;

// TODO: use the same COMPILER_BRAND as hir and concat at compile-time here
// use crate::COMPILER_BRAND;
// const MAIN_NAME: &'static str = concat!("main", COMPILER_BRAND);

impl VM {
    // pub(super) fn setup(&mut self) {
    //     self.allocate_main_function();
    // }

    // fn allocate_main_function(&mut self) {
    //     let main_chunk = &mut self.chunk;
    //     self.alloc_function(main_chunk, Some("main~"));
    //     //
    // }
}
